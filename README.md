# supervised_learning
This repository is a hands-on reference for supervised machine learning. It covers core algorithms — linear &amp; logistic regression, decision trees, random forests, SVMs, k-NN, and more — implemented both from scratch (using NumPy) and with Scikit-learn. 
mathematically we will see each algorithm
